#include <bits/stdc++.h>

using namespace std;

void solve(int t) {
    int N;
    cin >> N;
    string S;
    cin >> S;
    int risposta = 0;

    cout << "Case #" << t << ": " << risposta << "\n";
}

int main() {
    
    ifstream cin("input.txt");
    ofstream cout("output.txt");

    int T;
    cin >> T;

    for (int t = 1; t <= T; t++) {
        solve(t);
    }

    return 0;
}